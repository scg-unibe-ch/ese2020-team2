import { UserController } from '../../src/controllers/user.controller';
import { User } from '../../src/models/user.model';
import {and} from "sequelize";

/*

beforeEach(function(){
    jasmine.setDefaultSpyStrategy(and => and.returnValue("lol"))
});


describe("A suite is just a function", function() {

    it("and so is a spec", function(){
        const user1 = new User();

        const deleteUser = UserController.delete;
        const addUser = UserController.post;
        const editUser = UserController.put;
        const getUser = UserController.get;





        expect(user1.userId).toBe(true);
    });
});



describe("The 'toBe' matcher compares with ===", function() {


    it("and has a positive case", function () {
        expect(true).toBe(true);
    });

});
*/